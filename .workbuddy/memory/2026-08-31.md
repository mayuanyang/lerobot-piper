- 分析 LoRA 能否带来新能力：LoRA 是低秩残差（W+BA），只能做『域适配+注意力重路由』（如 wiltechs_x 学会双向前缀运行模式），不能让 VLM 长出新概念/新感知——天花板仍是 backbone 预训练智力。容量判断：wiltechs_x rank32/qkvo/36层 ≈5-8M 与 X-VLA 9M SOTA 同量级（域内够）；wilro 视觉 LoRA rank16/qv/8层 ≈49万仅为 DiT 0.12%（聊胜于无），paraphrase 0% 说明瓶颈在编码器智力而非适配容量，加 rank 无用。
