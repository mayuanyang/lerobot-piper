- 深度解析 flow matching 原理与推理：核对源码确认两模型同一约定——训练 x_t = t·noise + (1-t)·actions（t=0 干净/t=1 噪声，与多数论文相反），速度目标 u_t = noise - actions 沿路径为常量，MSE 最优解 = 条件期望 E[u|x_t]（单路径直线但边缘平均场弯曲，故推理需多步 Euler）；推理 dt=-1/N 从 t=1 积分回 t=0，wilro N=10 全跑 DiT（flow_actions_from_noise 可微供 GRPO），wiltechs_x N=4 shortcut+prefix KV cache 复用；t 采样 clamp [0.001,0.999]，支持 lognormal 偏置低 t（fine-detail denoising），换时间采样会移动 loss 基线须用 eval 判断。
