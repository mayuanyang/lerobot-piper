# Core ML — torch and torchvision are pre-installed in Colab, listed here for reference
# torch>=2.2.1,<2.8.0
# torchvision>=0.21.0,<0.23.0

# Version constraint required by LeRobot
numpy<2.0.0

# LeRobot 0.4.0 with SmolVLA support (pulls in transformers, safetensors, huggingface_hub, datasets)
lerobot[smolvla]==0.4.0

# Pinned to lerobot 0.4.0 constraints
huggingface_hub[cli,hf-transfer]>=0.34.2,<0.36.0
datasets>=4.0.0,<4.2.0
transformers>=4.53.0,<5.0.0
safetensors>=0.4.3,<1.0.0

# LoRA fine-tuning (peft pulled by lerobot[groot], but we pin torchao explicitly)
peft>=0.13.0,<1.0.0
torchao>=0.16.0

# Vision / data
opencv-python>=4.5.0
Pillow>=9.0.0
pyarrow>=14.0.0

# Optional: zero-shot object detection (only needed if using YOLO detector)
# ultralytics>=8.0.0
